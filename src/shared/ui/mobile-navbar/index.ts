import {
  Root,
  List,
  Trigger,
  Icon,
  Label,
  type State,
  type RootProps,
  type ListProps,
  type TriggerProps,
  type IconProps,
  type LabelProps,
  type Item,
} from "./mobile-navbar";

// Export all components as a single object
export const MobileNavbar = {
  Root,
  List,
  Trigger,
  Icon,
  Label,
};

// Export types separately
export type {
  State,
  RootProps,
  ListProps,
  TriggerProps,
  IconProps,
  LabelProps,
  Item,
};
